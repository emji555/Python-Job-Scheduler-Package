[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "workloom"
version = "0.1.0"
description = "Laravel-inspired jobs, queues, scheduling and monitoring for Python — framework and backend agnostic."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
  { name = "Workloom Maintainers", email = "maintainers@example.com" },
]
keywords = [
  "jobs",
  "queues",
  "scheduler",
  "celery",
  "background-tasks",
  "retries",
  "middleware",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Typing :: Typed",
]
dependencies = [
  "click>=8.1",
  "croniter>=2.0",
  "tzdata>=2024.1; platform_system == 'Windows'",
]

[project.optional-dependencies]
redis = ["redis>=5.0"]
celery = ["celery>=5.3", "redis>=5.0"]
django = ["django>=4.2"]
fastapi = ["fastapi>=0.110", "uvicorn>=0.27"]
flask = ["flask>=3.0"]
otel = ["opentelemetry-api>=1.24"]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "pytest-cov>=5.0",
  "ruff>=0.6",
  "mypy>=1.11",
  "pre-commit>=3.7",
  "build>=1.2",
  "twine>=5.1",
  "types-croniter",
  "types-redis",
]
docs = [
  "mkdocs-material>=9.5",
  "mkdocstrings[python]>=0.25",
]
all = [
  "workloom[redis,celery,django,fastapi,flask,otel]",
]

[project.urls]
Homepage = "https://github.com/emji555/Python-Job-Scheduler-Package"
Documentation = "https://emji555.github.io/Python-Job-Scheduler-Package/"
Repository = "https://github.com/emji555/Python-Job-Scheduler-Package"
Issues = "https://github.com/emji555/Python-Job-Scheduler-Package/issues"
Changelog = "https://github.com/emji555/Python-Job-Scheduler-Package/blob/main/CHANGELOG.md"

[project.scripts]
workloom = "workloom.cli.main:main"

[project.entry-points."workloom.backends"]
eager = "workloom.backends.eager:EagerBackend"
memory = "workloom.backends.memory:MemoryBackend"
celery = "workloom.backends.celery:CeleryBackend"

[project.entry-points."workloom.schedulers"]
inprocess = "workloom.schedulers.inprocess:InProcessScheduler"

[project.entry-points."workloom.serializers"]
json = "workloom.serializers.json:JsonSerializer"

[project.entry-points."workloom.storage"]
memory = "workloom.storage.memory:MemoryStorage"

[project.entry-points."workloom.locks"]
memory = "workloom.locks.memory:MemoryLockBackend"
redis = "workloom.locks.redis:RedisLockBackend"

[tool.hatch.build.targets.wheel]
packages = ["src/workloom"]

[tool.hatch.build.targets.sdist]
include = [
  "/src",
  "/tests",
  "/docs",
  "/examples",
  "/README.md",
  "/LICENSE",
  "/CHANGELOG.md",
  "/pyproject.toml",
]

[tool.pytest.ini_options]
minversion = "8.0"
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
  "integration: tests requiring external services",
  "celery: celery backend tests",
  "redis: redis-backed tests",
]

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["src", "tests", "examples"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]
ignore = ["E501", "UP035"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true
warn_unused_ignores = false
mypy_path = "src"
packages = ["workloom"]
exclude = ["examples/", "benchmarks/", "integrations/"]

[[tool.mypy.overrides]]
module = [
  "celery.*",
  "django.*",
  "fastapi.*",
  "flask.*",
  "redis.*",
  "croniter.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
  "workloom.integrations.*",
  "workloom.backends.celery",
  "workloom.locks.redis",
  "workloom.cli.*",
]
ignore_errors = true

[[tool.mypy.overrides]]
module = ["workloom.pipelines.*"]
disable_error_code = ["no-any-return", "unused-ignore"]

[tool.coverage.run]
branch = true
source = ["workloom"]

[tool.coverage.report]
show_missing = true
skip_covered = false
omit = ["*/integrations/*"]
